# Git Quick Start Guide
## Быстрый старт для paris-2026

**Версия**: 2.0  
**Дата**: 2025-10-26

---

## 🚀 Ежедневные команды

### Начало работы над новой фичей

```bash
# Автоматически: checkout develop → pull → create feature branch
git feat <название>

# Пример
git feat booking-form
# Создаст ветку: feature/booking-form
```

### Начало работы над исправлением

```bash
git fix <название>

# Пример
git fix mobile-menu
# Создаст ветку: fix/mobile-menu
```

### Коммит изменений

```bash
# Добавить файлы
git add .

# Коммит (откроется редактор с шаблоном)
git commit

# Или быстрый коммит
git commit -m "feat(form): добавить валидацию email"
```

### Push изменений

```bash
# Первый push (автоматически создаст upstream)
git push

# Или явно
git push origin feature/booking-form
```

### Красивый лог

```bash
# Графический лог со всеми ветками
git lg

# Простой лог
git ls

# С изменениями файлов
git ll
```

---

## 📦 Частые сценарии

### Обновить текущую ветку из develop

```bash
git checkout develop
git pull
git checkout -  # вернуться на предыдущую ветку
git rebase develop
```

### Быстро исправить последний коммит

```bash
# Добавить забытый файл
git add forgotten-file.js
git amend

# Или изменить сообщение
git amend-edit
```

### Посмотреть что изменилось

```bash
# Изменения в рабочей директории
git d

# Изменения в staged
git ds
```

### Отменить изменения

```bash
# Отменить uncommitted изменения в файле
git restore <file>

# Убрать файл из staged
git unstage <file>

# Отменить последний коммит (сохранить изменения)
git undo

# Отменить последний коммит (удалить изменения)
git undo-hard
```

### Временно сохранить изменения

```bash
# Сохранить
git save "Описание WIP"

# Вернуть
git pop
```

---

## 🔍 Проверка статуса

### Текущее состояние

```bash
git st
```

### Мои коммиты за сегодня

```bash
git today
```

### Мои коммиты за неделю

```bash
git week
```

### Все ветки

```bash
git branches
```

---

## 🧹 Очистка

### Удалить merged ветки

```bash
git cleanup
```

---

## 🆘 Типичные проблемы

### Забыл обновить develop перед созданием ветки

```bash
# В feature ветке
git checkout develop
git pull
git checkout -
git rebase develop
```

### Хочу взять коммит из другой ветки

```bash
git cherry-pick <commit-hash>
```

### Нужно переключиться, но есть uncommitted changes

```bash
# Сохранить изменения
git save "WIP"

# Переключиться
git checkout другая-ветка

# Работа...

# Вернуться
git checkout исходная-ветка

# Восстановить изменения
git pop
```

### Pre-commit hook блокирует коммит

```bash
# Прочитать сообщение ошибки
# Исправить проблему
# Попробовать снова

# Или (РЕДКО!) пропустить hook
git commit --no-verify -m "сообщение"
```

### Не могу запушить в main

```bash
# Это нормально! main защищен
# Используйте Pull Request через develop

# Только для hotfix:
ALLOW_MAIN_PUSH=1 git push
```

---

## 📝 Формат commit message

```
<type>(<scope>): <subject>

<optional body>

<optional footer>
```

### Типы (type)

- `feat` - новая функциональность
- `fix` - исправление бага
- `docs` - документация
- `style` - стили, форматирование
- `refactor` - рефакторинг
- `perf` - оптимизация
- `test` - тесты
- `build` - build система
- `ci` - CI/CD
- `chore` - рутина

### Примеры

```bash
feat(form): добавить валидацию email
fix(i18n): исправить RTL для арабского
docs: обновить README
style(hero): увеличить размер заголовка
refactor(build): упростить парсер
```

---

## 🎯 Workflow

### Новая фича (полный цикл)

```bash
# 1. Создать ветку
git feat booking-validation

# 2. Работа + коммиты
git add .
git commit

# 3. Push
git push

# 4. Создать PR на GitHub
#    feature/booking-validation → develop

# 5. После merge
git checkout develop
git pull
git cleanup  # удалит merged ветку
```

### Исправление бага

```bash
# 1. Создать ветку
git fix mobile-z-index

# 2. Исправить + коммит
git add style.css
git commit -m "fix(design): исправить z-index меню"

# 3. Push + PR
git push
# Создать PR → develop

# 4. После merge
git checkout develop
git pull
git cleanup
```

### Критический hotfix

```bash
# 1. Создать из main
git hot v1.2.1-form-crash

# 2. Исправить
git add index.html
git commit -m "fix(form): исправить crash"

# 3. Merge в main
git checkout main
git merge --no-ff hotfix/v1.2.1-form-crash
git tag -a v1.2.1 -m "Hotfix: form crash"

# 4. Push в main
ALLOW_MAIN_PUSH=1 git push origin main --tags

# 5. Merge в develop
git checkout develop
git merge --no-ff hotfix/v1.2.1-form-crash
git push

# 6. Очистка
git branch -d hotfix/v1.2.1-form-crash
git push origin --delete hotfix/v1.2.1-form-crash
```

---

## 📚 Дополнительно

### Полная документация

- `GIT_ARCHITECTURE.md` - Полная архитектура
- `GIT_WORKFLOW.md` - Детальный workflow
- `GIT_SETUP.md` - Настройка

### Помощь

```bash
# Помощь по команде
git <command> --help

# Например
git commit --help
git rebase --help
```

### Проверка конфигурации

```bash
# Посмотреть все alias
git config --get-regexp alias

# Посмотреть user info
git config user.name
git config user.email
```

---

## ✨ Pro Tips

1. **Используйте алиасы** - `git feat` быстрее чем полная команда
2. **Коммитьте часто** - маленькие атомарные коммиты лучше
3. **Проверяйте что коммитите** - `git d` перед `git add`
4. **Пишите хорошие сообщения** - будущее "вы" скажет спасибо
5. **Следуйте hooks** - они защищают от ошибок
6. **Синхронизируйте develop** - перед созданием веток
7. **Удаляйте merged ветки** - `git cleanup` регулярно

---

**Все готово!** 🎉  
Используйте этот гайд как шпаргалку.

Для деталей смотрите `GIT_ARCHITECTURE.md`.
