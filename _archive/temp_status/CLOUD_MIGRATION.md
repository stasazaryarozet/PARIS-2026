# МИГРАЦИЯ В ОБЛАКО CURSOR

**Дата**: 2025-10-29  
**Статус**: Готово к облаку

---

## ПРОВЕРКА ПЕРЕД МИГРАЦИЕЙ

### ✅ Относительные пути

Все скрипты используют относительные пути:
- `tools/manage_contacts_mvd.py`: `Path(__file__).parent.parent` → работает везде
- `build.py`: относительные пути к файлам проекта
- `server/app/main.py`: относительные пути

### ✅ Зависимости

Проверка зависимостей (только необходимые):
- Python 3.8+ (стандарт в облачных средах)
- Зависимости в `server/requirements.txt` (если используется сервер)

### ✅ Файлы проекта

Все необходимые файлы в репозитории:
- `ADDRESS_LIST.csv` — список адресатов (MVD, 3 поля)
- `WEBSITE_CONTENT.md` — источник правды для сайта
- `build.py` — генератор контента
- `tools/` — все скрипты с относительными путями

---

## ЧТО ИЗМЕНИТСЯ

### Абсолютные пути в документации

В документации (`*.md` файлах) остались примеры с абсолютными путями:
- `AI_COMMUNICATION_LOG.md`
- `TASK_CONTACTS_SYSTEM.md`
- Другие `.md` файлы

**Это нормально** — это примеры для чтения, не исполняемый код.

### Локальные артефакты (НЕ синхронизируются)

Эти файлы останутся локально (не критичны для работы):
- `reasoning.log` — локальный лог, создастся заново в облаке
- `__pycache__/` — кэш Python, пересоздастся
- `*.pyc` — скомпилированные файлы, пересоздадутся

---

## ПРОВЕРКА РАБОТЫ В ОБЛАКЕ

### После открытия проекта в облаке Cursor:

1. **Проверить Python**:
```bash
python3 --version  # Должен быть 3.8+
```

2. **Проверить скрипты**:
```bash
cd tools
python3 manage_contacts_mvd.py  # Должен работать
```

3. **Проверить генерацию сайта**:
```bash
python3 build.py  # Должен работать
```

4. **Проверить список адресатов**:
```bash
cat ADDRESS_LIST.csv  # Должен показать 20 контактов
```

---

## ОСОБЕННОСТИ ОБЛАКА

### Пути к файлам

В облаке Cursor рабочая директория будет автоматически определена как корень проекта. Все относительные пути работают как обычно.

### iCloud Drive файлы

Если проект был в iCloud Drive (`~/Library/Mobile Documents/com~apple~CloudDocs/Projects/paris-2026`), после миграции в облако Cursor файлы будут в облачном хранилище Cursor, а не в iCloud.

**Проверка**: После открытия в облаке Cursor все файлы должны быть доступны.

---

## КРИТИЧЕСКИЕ ФАЙЛЫ

### Обязательно синхронизировать:

1. `ADDRESS_LIST.csv` — список адресатов (20 контактов)
2. `WEBSITE_CONTENT.md` — источник правды
3. `build.py` — генератор
4. `index.html`, `style.css`, `content.js` — сайт
5. `OPERATIONAL_MODEL_PROPOSAL_v7.md` — операционная модель
6. `AI_COMMUNICATION_LOG.md` — координация AI
7. `TASK_CONTACTS_SYSTEM.md` — задачи
8. `tools/manage_contacts_mvd.py` — скрипт контактов

### Могут пересоздаться:

- `reasoning.log` — создастся заново
- `__pycache__/` — пересоздастся
- Все временные файлы

---

## БЫСТРЫЙ СТАРТ В ОБЛАКЕ

После открытия проекта в облаке Cursor:

```bash
# 1. Проверка
python3 --version
ls -la ADDRESS_LIST.csv

# 2. Тест скрипта контактов
python3 tools/manage_contacts_mvd.py

# 3. Тест генерации сайта
python3 build.py

# 4. Готово
```

---

**Shout Out to Speedy Boat the Laptop!** 🚤

Проект готов к работе в облаке Cursor без привязки к локальному компьютеру.

