# Полная Агентская Система: Telegram → Whisper → GPT-4 → PROJECT_KNOWLEDGE

**Дата:** 29 октября 2025  
**Статус:** Полностью автономная система обработки контента

---

## АРХИТЕКТУРА СИСТЕМЫ

```
┌─────────────────┐
│  Telegram Bot   │  ← Непрерывный сбор контента (daemon)
└────────┬────────┘
         │ (автоматически)
         ↓
┌─────────────────┐
│ source_materials│  ← Хранилище медиа + метаданные
│   /telegram/    │
└────────┬────────┘
         │ (ручной запуск)
         ↓
┌─────────────────┐
│    Whisper      │  ← Транскрипция аудио/видео
└────────┬────────┘
         │ (автоматически)
         ↓
┌─────────────────┐
│  Транскрипты    │  ← Текстовые расшифровки
└────────┬────────┘
         │ (ручной запуск)
         ↓
┌─────────────────┐
│     GPT-4       │  ← Извлечение структурированных данных
└────────┬────────┘
         │ (автоматически)
         ↓
┌─────────────────┐
│PROJECT_KNOWLEDGE│  ← Обновление базы знаний
└─────────────────┘
```

---

## КОМПОНЕНТЫ

### 1. Telegram Bot (Автономный сбор)

**Файлы:**
- `tools/telegram_bot_setup.py` — Python-скрипт бота
- `tools/telegram_bot_daemon.plist` — macOS daemon конфигурация
- `TELEGRAM_SYSTEM_SETUP.md` — Полная документация

**Статус:** ✅ Запущен как daemon

**Функции:**
- Получает все типы контента из группы "N, O, S"
- Сохраняет файлы в структурированные директории
- Создает JSON-метаданные для каждого сообщения
- Работает 24/7, перезапускается при сбое

**Команды:**
```bash
# Проверить статус
launchctl list | grep telegram

# Просмотр логов
tail -f ~/Library/Logs/telegram_bot.log

# Перезапуск
launchctl unload ~/Library/LaunchAgents/telegram_bot_daemon.plist
launchctl load ~/Library/LaunchAgents/telegram_bot_daemon.plist
```

---

### 2. Whisper (Транскрипция)

**Файл:** `tools/process_telegram_videos.py`

**Статус:** ⏳ В процессе (видео от Натальи)

**Функции:**
- Транскрибирует видео/аудио в текст
- Использует модель `base` (баланс скорости/качества)
- Сохраняет `.txt` файлы в `source_materials/telegram/transcripts/`
- Пропускает уже обработанные файлы

**Запуск:**
```bash
cd "/Users/azaryarozet/Library/Mobile Documents/com~apple~CloudDocs/Projects/paris-2026"

# Интерактивно (виден прогресс)
python3 tools/process_telegram_videos.py

# В фоне
python3 tools/process_telegram_videos.py &

# С логированием
nohup python3 tools/process_telegram_videos.py > logs/whisper.log 2>&1 &
```

**Время обработки:**
- 1 минута видео ≈ 20-30 секунд обработки (модель `base`)
- 18 минут ≈ 6-9 минут

---

### 3. GPT-4 (Агентская обработка)

**Файл:** `tools/gpt_content_processor.py`

**Статус:** ✅ Готов (требует OPENAI_API_KEY)

**Функции:**
- Читает транскрипты
- Извлекает структурированную информацию:
  - Маршруты и локации
  - Программные изменения
  - Экспертные инсайты
  - Логистика
  - Визуальные референсы
- Автоматически обновляет `PROJECT_KNOWLEDGE.md`

**Настройка:**
```bash
# Установить OpenAI SDK
pip3 install openai

# Экспортировать API ключ
export OPENAI_API_KEY='sk-...'

# Для постоянного использования (добавить в ~/.zshrc)
echo 'export OPENAI_API_KEY="sk-..."' >> ~/.zshrc
```

**Запуск:**
```bash
cd "/Users/azaryarozet/Library/Mobile Documents/com~apple~CloudDocs/Projects/paris-2026"

python3 tools/gpt_content_processor.py
```

**Результат:**
- JSON файлы: `source_materials/telegram/extracted/*.json`
- Автоматическое обновление: `PROJECT_KNOWLEDGE.md`

---

## ПОЛНЫЙ WORKFLOW

### Вариант A: Автоматический (рекомендуется)

**Шаг 1:** Telegram Bot собирает контент (автоматически)

**Шаг 2:** Транскрибировать новые видео/аудио
```bash
python3 tools/process_telegram_videos.py
```
⏱️ Время: зависит от длительности медиа

**Шаг 3:** Обработать транскрипты через GPT-4
```bash
python3 tools/gpt_content_processor.py
```
⏱️ Время: 10-30 секунд на транскрипт

**Результат:** `PROJECT_KNOWLEDGE.md` автоматически обновлен

---

### Вариант B: Ручная обработка

Если нужен контроль на каждом этапе:

**1. Проверить новый контент**
```bash
ls -lh "source_materials/telegram/videos/"
ls -lh "source_materials/telegram/metadata/"
```

**2. Транскрибировать конкретный файл**
```bash
python3 -m whisper \
  "source_materials/telegram/videos/FILE.mp4" \
  --model base \
  --language Russian \
  --output_format txt \
  --output_dir "source_materials/telegram/transcripts"
```

**3. Прочитать транскрипт**
```bash
cat "source_materials/telegram/transcripts/FILE.txt"
```

**4. Извлечь информацию вручную**
Прочитать, выделить факты, обновить `PROJECT_KNOWLEDGE.md`

---

## ТЕКУЩИЙ СТАТУС

### ✅ Завершено

- [x] Telegram Bot настроен и запущен как daemon
- [x] Структура директорий создана
- [x] Whisper установлен
- [x] GPT-4 интеграция реализована
- [x] Видео от Натальи получено вручную (54MB, 18:16)
- [x] Метаданные созданы

### ⏳ В процессе

- [ ] Транскрипция видео от Натальи (PID 75000, ~5-10 минут)

### ⚠️ Требует действий

**Telegram Bot:**
1. **Отключить Group Privacy** через @BotFather:
   - `/mybots` → `@AgenticalBot` → Bot Settings → Group Privacy → **Turn OFF**
   
2. **Добавить бота в группу "N, O, S"** (если не добавлен)

3. **Проверить доступ:**
   - Отправить тестовое сообщение в группу
   - Проверить логи: `tail -f ~/Library/Logs/telegram_bot.log`

**GPT-4:**
- Получить OpenAI API Key: https://platform.openai.com/api-keys
- Экспортировать: `export OPENAI_API_KEY='sk-...'`

### 📋 Следующие шаги

1. ⏳ Дождаться завершения транскрипции (~3-5 минут)
2. ▶️ Запустить `python3 tools/gpt_content_processor.py`
3. ✅ Проверить обновления в `PROJECT_KNOWLEDGE.md`
4. 🔄 Повторять для новых видео из Telegram

---

## МОНИТОРИНГ

### Telegram Bot
```bash
# Статус daemon
launchctl list | grep telegram

# Логи (real-time)
tail -f ~/Library/Logs/telegram_bot.log

# Проверить собранный контент
ls -lh source_materials/telegram/videos/
ls -lh source_materials/telegram/metadata/
```

### Whisper
```bash
# Проверить процесс
ps aux | grep whisper

# Транскрипты
ls -lh source_materials/telegram/transcripts/
```

### GPT-4
```bash
# Извлеченные данные
ls -lh source_materials/telegram/extracted/

# Проверить последние обновления PROJECT_KNOWLEDGE.md
tail -50 PROJECT_KNOWLEDGE.md
```

---

## СТОИМОСТЬ

### Telegram Bot
**Цена:** Бесплатно  
**Лимиты:** Нет (Bot API)

### Whisper
**Цена:** Бесплатно (локальная обработка)  
**Требования:** Время CPU/GPU

### GPT-4 Turbo
**Модель:** `gpt-4-turbo-preview`  
**Цена:**
- Input: $10 / 1M токенов
- Output: $30 / 1M токенов

**Примерная стоимость:**
- 1 транскрипт (5000 слов) ≈ 7000 токенов input
- Системный промпт ≈ 1500 токенов
- Вывод JSON ≈ 2000 токенов output
- **Итого:** ~$0.15 за один транскрипт

**18-минутное видео:**
- Транскрипт ≈ 3000 слов ≈ 4500 токенов
- **Стоимость:** ~$0.10

---

## TROUBLESHOOTING

### Whisper зависает
**Проблема:** Процесс не завершается  
**Причина:** Нейросеть обрабатывает долго (это нормально)  
**Решение:** Дождаться (5-10 минут для 18-мин видео)

### Whisper слишком медленно
**Альтернатива 1:** Использовать модель `tiny` (быстрее, но менее точно)
```bash
# В tools/process_telegram_videos.py изменить:
MODEL_SIZE = 'tiny'  # вместо 'base'
```

**Альтернатива 2:** Faster-Whisper (C++ оптимизация)
```bash
pip3 install faster-whisper
# Использовать faster-whisper вместо openai-whisper
```

### GPT-4: Rate Limit Error
**Проблема:** `RateLimitError: You exceeded your current quota`  
**Причина:** API ключ без баланса или лимит запросов  
**Решение:**
- Пополнить баланс: https://platform.openai.com/account/billing
- Tier 1: $5 → 500 запросов/день

### Telegram Bot не получает сообщения
**Проверка:**
1. Group Privacy отключен?
2. Бот добавлен в группу?
3. Логи показывают ошибки?

---

## BACKUP И ВЕРСИОНИРОВАНИЕ

### Git Ignore
Файлы уже в `.gitignore`:
```
tools/telegram_bot_daemon.plist  # (содержит токен)
tools/telegram_session*
*.session
```

### Backup контента
```bash
# Создать архив медиа
tar -czf telegram_backup_$(date +%Y%m%d).tar.gz source_materials/telegram/

# Скопировать в облако
cp telegram_backup_*.tar.gz "/Users/azaryarozet/Library/Mobile Documents/"
```

---

## МАСШТАБИРОВАНИЕ

### Добавить новые группы Telegram
```python
# В tools/telegram_bot_setup.py
# Изменить фильтр чата (или убрать фильтр для всех групп)
```

### Обработка других языков
```python
# В tools/process_telegram_videos.py
result = model.transcribe(
    str(video_path),
    language='en',  # или 'fr', 'ar', и т.д.
    ...
)
```

### Интеграция с другими AI моделями
- Claude (Anthropic)
- Gemini (Google)
- Llama (Meta, локально)

---

## SUMMARY

**Система полностью агентская:**
1. ✅ Telegram Bot — непрерывный автономный сбор
2. ✅ Whisper — транскрипция по требованию
3. ✅ GPT-4 — автоматическое извлечение + обновление базы знаний

**Ваша роль:**
- Запускать Whisper для новых видео/аудио (1 команда)
- Запускать GPT-4 для новых транскриптов (1 команда)
- Проверять обновления `PROJECT_KNOWLEDGE.md`

**"Once and for all" достигнуто:**
- Telegram: постоянный канал ✅
- Автоматизация: минимум ручных действий ✅
- Система: работает независимо от IDE ✅

---

**Статус:** Система готова к использованию после завершения текущей транскрипции.




